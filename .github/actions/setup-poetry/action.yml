name: 'Setup Poetry'
description: 'Installs Poetry and dependencies'
runs:
  using: 'composite'
  steps:
    - name: Install Poetry
      uses: snok/install-poetry@v1

    - name: Install dependencies
      shell: bash
      run: |
        poetry install
